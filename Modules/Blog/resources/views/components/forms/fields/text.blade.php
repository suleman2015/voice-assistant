<div class="row mb-3">
    <div class="col-12">
        <label for="{{ $name }}" class="form-label"><strong>{{ $label }}</strong></label>
        <input 
            type="text" 
            class="form-control" 
            name="{{ $name }}" 
            id="{{ $name }}" 
            value="{{ $value }}" 
            placeholder="{{ $placeholder }}">
    </div>
</div>